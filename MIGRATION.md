# Migration provenance

The replacement release candidate uses the read-only legacy repository `JEAN-MICHEL-REPOS` at exact commit `186619db09e3d8d693b90f7a38fae079bec0775b` as visual and voice evidence.

The obsolete standalone CLI-only candidate is preserved locally on `archive/open-clowk-obsolete-cli-rc-20260806` and is not part of this branch history. This implementation starts from standalone `origin/main` at `71a87e4a140711b3c832cf227a72815f930b9ae6` and carries over only the required captain-owned Open Clowk artwork and harmless voice.

The legacy repository was not modified.
